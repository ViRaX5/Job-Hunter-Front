# Set this once the backend is reachable from the internet (e.g. via
# Tailscale, Cloudflare Tunnel, or a public domain), then push - the GitHub
# Actions build picks this up automatically. Until it's set, the deployed
# page will load but job data will fail to fetch (expected until the
# backend is actually deployed).
VITE_API_URL=
