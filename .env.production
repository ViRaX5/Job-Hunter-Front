# TEMPORARY: Cloudflare quick tunnel to the backend running on the Windows
# laptop, until the Linux box is set up. This URL changes every time the
# tunnel restarts (laptop reboot, cloudflared restart, etc.) - when that
# happens, get the new URL from the cloudflared output and update this line,
# then commit + push to trigger a rebuild. Once the Linux backend is live,
# replace this with its permanent address instead.
VITE_API_URL=https://century-spending-periodic-install.trycloudflare.com
